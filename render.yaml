services:
  - type: web
    name: socket-chat-server
    env: docker
    dockerfilePath: ./server/Dockerfile
    region: oregon
    plan: starter
    disk:
      name: sqlite-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 55555
      - key: HOST
        value: 0.0.0.0
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_PATH
        value: /app/data/users.db
